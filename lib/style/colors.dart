import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF303030); // Primary color
  static const Color accentColor = Color(0xFF509AE6);  // Accent color (secondary)
  static const Color text_mainColor = Color(0xFFFFFFFF);
}